import React from "react";
import { Box } from "@mui/material";
import { useRouter } from "next/router";

const DiscoverDetails = () => {
  const router = useRouter();
  console.log(router);

  return <Box>thsi si disoc</Box>;
};

export default DiscoverDetails;
